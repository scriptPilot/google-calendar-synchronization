{
  "version": "4.2.0",
  "scripts": {
    "start": "clasp push && clasp open",
    "build": "npx prettier scripts src --write && node scripts/build"
  },
  "dependencies": {
    "glob": "^11.0.1",
    "prettier": "^3.4.2"
  }
}
